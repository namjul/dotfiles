test -d ~/.dotfiles/bin && set PATH $PATH ~/.dotfiles/bin
test -d ~/bin && set PATH $PATH ~/bin
test -d ~/.local/bin && set PATH $PATH ~/.local/bin
test -d ~/.cargo/bin && set PATH $PATH ~/.cargeo/bin
