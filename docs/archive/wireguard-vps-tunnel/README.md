# Archived: WireGuard VPS ↔ desktop tunnel

Historical operator notes for exposing a local desktop service through a VPS public IP. **Not deployed** from this repo anymore (removed from `aspects/server/files/srv/square/archive/wireguard/` in favor of Tailscale on the homelab).

Desktop/laptop WireGuard client config may still live under dotfiles encrypted paths (`aspects/dotfiles`); that is separate from the retired server aspect.
