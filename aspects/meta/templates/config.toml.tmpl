# Generated by Fig Meta
# Platform: {{ .platform }}
# Hostname: {{ .hostname }}
# User: {{ .user }}

[settings]
editor = "{{ .editor }}"
shell = "{{ .shell }}"

[platform.{{ .platform }}]
package_manager = "{{ .package_manager }}"
