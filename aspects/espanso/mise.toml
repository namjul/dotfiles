[tasks.default]
description = "Compile Espanso for Wayland and enable the user unit (not on install.sh)"
usage = '''
flag "--force" help="Rebuild even if this version is already installed"
'''
file = "default"
