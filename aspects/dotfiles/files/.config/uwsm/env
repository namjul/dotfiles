# UWSM sources this file, then env.d/* (see `man uwsm` CONFIGURATION).
# Keep this file for notes only; fragments live in env.d/, session defaults in `default`.
# Compositor client env (GTK/Qt/…): niri config.kdl `environment { }`.
