---
description: OntoEdit rubric analysis — level, SS band, PS standards, flags, conjunctive arguments
argument-hint: "[scope] <passage>"
---

Use the `ontoedit-analysis` skill. Pin rubric version to the skill default.

**Scope** (default `full`): optional first token or `scope: <name>` in the arguments. Allowed values: `full`, `level`, `ss`, `ps`, `flags`, `relation`. Everything else is the passage under analysis. For `relation`, include both domains or an explicit comparison in the passage text.

User arguments:

$ARGUMENTS
