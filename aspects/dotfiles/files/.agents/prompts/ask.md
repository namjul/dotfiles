---
description: Answer a question; do not change files
---

Answer `$ARGUMENTS`. Do not edit files, create files, or implement changes.

Reading and searching the repo (or running read-only commands) is allowed when needed to answer accurately. Separate verified facts from inference; label guesses when the answer is not fully grounded.

If the user asks for implementation, fixes, or commits, answer in prose only and point them to `/code`, `/plan`, or a follow-up that explicitly requests changes.

$ARGUMENTS
